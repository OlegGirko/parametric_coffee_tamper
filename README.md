# Parametric coffee tamper in OpenSCAD

This project contains OpenSCAD module for parametric coffee tamper.
All necessary parameters (like diameter, handle size etc.) can be
specified as module arguments.

